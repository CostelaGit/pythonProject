'''Dada a função seno, crie um programa que imprima a seguinte sequencia:

Essa sequência pode ser definida como:
variando-se um parâmetro (p) entre 1 e -1 (em intervalos de 0.1), gera-se n (digamos 160) valores da função
seno entre 0 e 16 em intervalos de 0.1. Para cada valor de p, instancia-se uma string (s) de valor "", essa string
irá conter o valor da linha a ser impressa. Se o parâmetro p for maior ou igual a zero, s será concatenada com
o caractere * (s = s + "*") se a função seno for maior que p, caso contrário s será concatenada com espaço
vazio (s = s + " "). Se o parâmetro p for menor que zero, s será concatenada com o caractere * (s = s + "*") se
a função seno for menor que p, caso contrário s será concatenada com espaço vazio (s = s + " ").'''